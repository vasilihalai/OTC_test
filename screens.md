# xRuby OTC — screens.md

Reverse-documented from the current implementation (`/Users/vasilihalai/Desktop/OTC`) as of 2026-08-20. This describes what's actually built and live, not a design intent — use it as the baseline for the next round of changes. Supersedes the 2026-08-19 version: that one predates the full-bleed panel-layout fix, the Profile rebuild, this round's polish pass, and the TransferModal rebuild against its Figma frame (all folded in below).

## 1. Scope

Telegram Mini App (React + TypeScript + Vite, HashRouter, zustand). Mock-first: every screen reads/writes through `src/api/index.ts`, which resolves each function to either `src/api/mock/*` or `src/api/real/*` behind `VITE_USE_REAL_API`. **`api-integration.md`'s entire numbered work plan (§9) is now implemented against `real/*`** — auth, profile, balances, both withdrawal halves, account transfer, the certificate download, and the full OTC read/write/polling path (deals never stay mock-only behind the flag any more; `mockActions.expireQuote` is the one deliberate exception, a client-only countdown-driven state change with no server command to mirror). No live backend exists yet to reconcile these against, though — every `real/*` module is built to the doc's literal contract, flagged per-module where the doc itself flagged an open question (the `B1`–`B13` codes threaded through code comments and §8 below), and verified via `tsc`/lint/build plus a full mock-mode Playwright pass each round; the real-mode fetch shapes themselves are unverified against an actual server.

**Auth has been rebuilt three times now — see §5 for the current shape, this is just the timeline.** Round 1: a Telegram-initData-binding flow (`session.start`/`login`/`login-confirm`/`logout`) per an early spec document, `initData` itself the credential, sign-in only ever appearing once per device — deleted (`real/session.ts`, `LoginConfirmModal`, an `AuthError` boot-blocking screen, `bootRealSession`, the personal/business two-different-backends `config.ts`). Round 2, against `api-integration.md` §0/§2: ordinary email+password+OTP on every app open, a real refresh token, `initData` demoted to a one-shot push-binding header — explicitly *not* an auth mechanism. Round 3 (current): the backend team gave a concrete, different, real contract — a genuine Telegram-identity-binding endpoint again (conceptually closer to round 1, mechanically different), silent re-entry once bound, no refresh token at all for this client. `initData`'s one-shot push-binding header from round 2 is gone along with it. Each round was a full replacement, not an extension — confirmed with the user before touching code each time, since every round invalidated real work the previous one had done.

Dark theme only, regardless of the user's Telegram theme — Profile's theme/language pickers are cosmetic (see §6.9), they don't switch anything.

## 2. Design tokens

### 2.1 Colors (`src/theme/tokens.css`)

```
Surfaces
  --bg-page          #0C0E12   panel fill, field fill, modal fill
  --bg-surface       #13161B   cards nested inside a panel (details card, quote card, requisites panel, summary block, stat card)
  --bg-raised        #191C22   frame background behind full-bleed panels (the seam color), secondary/disabled buttons,
                                code cells, tab-segment track, transfer-modal fields, action-icon circles
                                — darkened from #22262F this round; was reading too light against --bg-page
  --bg-elevated      #22262F   new token, added a later round — #22262F itself turned out still needed for a couple
                                of specific elevated surfaces (Profile's certificate button, the active filter chip)
                                even after --bg-raised moved off it; kept as its own token rather than reverting
                                --bg-raised, since only those two spots' fresh references called for this lighter tone
  --border           #373A41
  --border-strong    #61656C
  --pill-scrim       rgba(65,63,64,.4)    AppHeader pill fill (+ backdrop-blur 12px)
  --modal-scrim      rgba(247,247,247,.1)

Text
  --text-primary     #F7F7F7
  --text-strong      #ECECED
  --text-body        #CECFD2   default body color (set on <body>)
  --text-muted       #94979C
  --text-subtle      #8B8F97
  --text-quiet       #85888E
  --text-disabled    #61656C
  --text-on-accent   #FFFFFF
  --text-on-light    #373A41   label color on white/primary buttons, enabled AND disabled

Accent / semantic
  --accent           #BB0023   primary red — CTAs, errors, danger links, active tab-bar icon
  --brand-mark       #C51625   small logo dot only
  --required         #E4002B   asterisk on required fields
  --positive         #039855
  --verified         #12B76A   check glyph inside the profile "Верифицирован" badge
  --badge-success-bg #022417 / --badge-success-text #027A48
  --warning-text     #B54708   unused since Profile's "Уровень безопасности" row was removed (§6.9) — token kept for whenever it's needed next
  --panel-warning    #241005   WarningPanel + Callout warning fill (darkened from #301003 — read as too bright/orange)
  --panel-error      #2F0904   Callout danger fill

Deal status (solid fills, keyed by the literal DealStatus string)
  DONE               fill #022417  text #027A48
  RUNNING            fill #05232F  text #0E6F90
  RATE_PENDING       fill #031332  text #004EEB
  RATE_ACTIVE        fill #170633  text #6927DA
  RATE_STALE         fill #13161B  text #94979C
  AWAITING_FUNDS     fill #301003  text #B54708
  DECLINED           fill #2F0904  text #BB0023

StatusHero gradient top-stops (per status, flat --bg-surface bottom half)
  RUNNING #05232F · DONE #022417 · DECLINED #2F0904 · RATE_STALE #22262F · RATE_PENDING #031332
```

### 2.2 Type scale (`src/theme/type.css`, classes `.t-*`)

| Class | Size/line | Weight |
|---|---|---|
| screen-title | 20/28 | 600 |
| screen-title-sm | 18/28 | 600 |
| section-title | 20/28 | 500 |
| stat | 24/32 | 600 |
| deal-id | 20/28 | 700 |
| amount-xl | 36/44 | 700 |
| field | 16/24 | 400 |
| button | 16/24 | 600 |
| total | 16/24 | 600 |
| body | 16/24 | 400 |
| label | 14/20 | 400 |
| label-strong | 14/20 | 600 |
| label-med | 14/20 | 500 |
| caption | 12/16 | 500 |

Font: Inter (self-hosted via `@fontsource/inter`, weights 400/500/600/700), `font-variant-numeric: tabular-nums` on `<body>`. Every text/password field renders its placeholder at the same 16px. `PasswordField`'s masked-dot sizing has gone back and forth several times (13px → 9px → "match Email's 16px" → smaller-dots-bigger-letters → **back to matching Email's 16px exactly, the current and — per the explicit instruction that landed it — final state**): no more conditional class, the masked and revealed states of the one toggled `<input>` (`type="password"`/`type="text"`) now share the exact same font-size/letter-spacing, full stop.

### 2.3 Layout model — full-bleed panels, not inset cards

This is the thing most likely to regress, so it's stated first. A panel on Home, the withdrawal screens, and Profile is **390-ish wide at x=0, no side margin, ever** — the inset lives entirely in the panel's own `padding`. `Panel` (`src/components/Panel/Panel.tsx`) takes `fill` (`page | raised | surface`), `radius` (CSS shorthand — rounded only on the seam-facing edge(s)), and `padding` props; it never takes a margin. Screens that need this stack panels with `gap: 16px` in a flex column and let each panel's own background show through the gap — that 16px gap *is* the seam.

- **Home**: root `display:flex; flex-direction:column; gap:16px; background: var(--bg-raised)`. Three panels, **all sharing the same 16px radius now** (hero and deposit-account were `24px` — explicit "make the card corners one consistent radius" instruction, the mismatch was most visible where the hero panel's bigger curve sat directly behind the two 16px `StatCard` boxes nested inside it): hero (`radius="0 0 16px 16px"`, `padding="8px 16px 20px"`, bg-page), recent-deals (`radius="16px"`, `padding="20px 0 4px"`, bg-page, heading/rows carry their own 16px horizontal inset), deposit-account (`radius="16px 16px 0 0"`, `padding="20px 0"`, bg-page, same pattern).
- **Deals**: no panel at all — frame is bg-page, title/filter-chips/rows sit directly on it, rows full-bleed to both screen edges.
- **DealDetail**: no panel either — frame bg-page, header/quote-card/hold-confirmation/`Детали`/`Документы` sit directly on it at a flat 16px inset. `Детали` and `Документы` are **plain heading + rows, not cards** — an earlier version wrapped them in a `--bg-surface` card, which doesn't match; only the quote card and `RequisitesPanel` are genuine cards (`Panel fill="surface" radius="16px"`).
- **Withdrawals** (WithdrawCrypto/WithdrawFiat/WithdrawRequisites): single implicit full-bleed panel — content inset 16px horizontal, 20px top, 16 or 32px bottom depending on screen, no radius (it's the only content on the screen, so the frame color is never actually visible).
- **Profile**: two full-bleed panels (`padding: 16px 16px 24px` / `16px`), bg-raised frame, 16px seam, panel2's `Тема`↔`FAQ` divider is inset 24px (8 more than the row content) — that's drawn intentionally, not a bug.

**Row components reach both edges themselves.** `DealRow` (128px, used on Home and Deals) and `TableRow` (64px asset row, used on Home's deposit-account list) are full-bleed with their own internal padding — never wrapped in a padded container that would double the inset. `TableRow` is a 3-cell flex row: info-cell (`padding-left:16px`, holds `CurrencyIcon` + ticker/name), amount-cell, action-cell (68px wide, `padding-right:16px`, holds a 36px round `--bg-raised` circle) — so the currency icon and the withdraw button both sit exactly 16px from the screen edge, symmetric. `DealRow` carries **no per-status background highlight** — a `RATE_ACTIVE` full-bleed highlight was tried, then explicitly reverted: it read as "the first item in the list is highlighted" rather than status-driven, which was confusing rather than informative. Its `.deal-row__id` text is **16px/22, weight 700** (was 14/20/600) — bumped per a Figma annotation, since the deal ID is the row's primary identifier and read as too light against the amount rows below it.

### 2.4 Motion — page transitions, modal open/close, loading

Reverse-documented after an explicit "go through the whole app, make every transition and loading state smoother, with nicer loaders" pass. The rule this pass settled on: **every overlay that animates in now also animates out**, and every loading affordance uses the same shimmer/spin visual language rather than each screen inventing its own.

- **Route/page transitions** (`.app-content__page`, `src/index.css`) — every screen mount still just fades + slides up 10px (`page-in`, was 6px/`ease-out`/0.18s), no exit animation (React Router's `key`-based remount unmounts the old page synchronously — a true crossfade would need to keep both pages mounted during the swap, judged not worth the complexity for a screen-to-screen nav in a HashRouter mobile app). The curve is now `cubic-bezier(0.2, 0.8, 0.3, 1)`, the same easing `Modal`/`PickerModal`/`TransferModal` already used for their entrances — one motion signature for the whole app instead of a plain `ease-out` that only page transitions used.
- **Modal / PickerModal exit animations** — both used to `return null` the instant `open` went false, so their entrance animation (fade+scale, fade+slide-up) had no closing counterpart: the overlay just vanished on the same frame. Fixed via a new shared hook, `useDelayedUnmount(open, exitDurationMs)` (`src/lib/useDelayedUnmount.ts`) — keeps the node mounted for `exitDurationMs` after `open` flips false so a CSS closing animation has time to play, then unmounts. Both components add a `--closing` modifier (`e('card', { closing: !open })` etc.) while in that window: `Modal` gets `modal-scrim-out`/`modal-card-out` (fade + scale to 0.96/translateY 4px, 0.16s `cubic-bezier(0.4, 0, 1, 1)` — an "ease-in" curve, since something leaving should accelerate away rather than ease out like something arriving), `PickerModal` gets the sheet-equivalent (fade + slide back down to `translateY(24px)`, same timing). Every modal in the app rides on these two shared components (`AuthenticatorModal`, `VerificationModal`, `OtpConfirmModal`, `TransferModal`, `ConfirmDialog` all wrap `Modal`; Profile's Язык/Тема pickers use `PickerModal`), so this one fix closes the "modal just disappears" gap everywhere at once. `QrScannerModal` was deliberately left out of this — it's a live camera overlay, and tearing down `getUserMedia` promptly on close matters more there than a pretty fade.
- **Skeleton** (`src/components/Skeleton/Skeleton.tsx`) — swapped the plain opacity pulse (`0.5↔1`, 1.2s) for a shimmer sweep: a `linear-gradient(100deg, --bg-surface 33%, --bg-elevated 50%, --bg-surface 67%)` animated across a `300% 100%` background-size, 1.6s ease-in-out loop. Reads as a much more "designed" loading state than a fade, and is the standard skeleton pattern most apps use now. One shared component, so this landed on every screen that shows skeletons at once (Home, Deals, DealDetail, WithdrawCrypto, WithdrawFiat, Profile) with no per-screen changes needed.
- **Spinner** (`src/components/Spinner/Spinner.tsx`, new) — the small `currentColor` ring spinner `Button`'s `loading` state already drew inline (`.button__spinner`) was pulled out into its own reusable component so other places needing a spinner don't hand-roll the same CSS again. `Button` now renders `<Spinner size={20}/>` instead of its own `<span>`; `Button.css`'s spinner rule/keyframe moved to `Spinner.css` (renamed `button-spin` → `spinner-spin`) since nothing else in `Button.css` needed it.
- **Button `loading` consistency audit** — two confirm buttons (`WithdrawCrypto`, `WithdrawRequisites`) disabled themselves during `submitting` but never passed `loading`, so tapping "Подтвердить" gave zero visual feedback while the withdrawal request was in flight (every other confirm/submit button in the app — SignIn, NewPassword, PasswordRecovery, DealDetail's actions — already did this correctly). Both now pass `loading={submitting}` alongside their existing `disabled` conditions.
- **OTP verification spinner** — the three code-entry modals (`VerificationModal`, `AuthenticatorModal`, `OtpConfirmModal`) disable `CodeInput` while `verifying`/`confirming` (that flag stays true through both `verifyCode()` *and* the awaited `onVerified()` callback, i.e. the actual withdrawal/login submission that runs after the code checks out) but showed nothing to indicate work was happening — cells just went inert. Each modal now shows a small `<Spinner size={16}/>` + "Проверяем код…" (`ru.verification.verifyingLabel`, new) directly below `CodeInput` while that flag is true, fading in over 0.15s.
- **Amount-field thousands grouping while typing** — see §3's `AmountField` entry.

## 3. Component library (`src/components/*`)

- **Panel** — the full-bleed primitive, see §2.3. `fill="page"` (default) / `"raised"` / `"surface"`; `radius`/`padding` as CSS shorthand strings, e.g. `radius="0 0 24px 24px"`.
- **Button** — variants `primary | accent | secondary | link | footer-link`, plus a `danger` boolean modifier (used with `link`) and a `size: 'regular' | 'compact'` (compact = 36px/radius 18/`button-sm` label, used only by Profile's certificate button). Height 44 regular, radius 30. Primary: white fill enabled, `--bg-raised` fill disabled, label stays `--text-on-light` in *both* states. Accent: `--accent` fill. **Secondary disabled is a flat `--border` fill now, no border, no `--bg-surface`** — the previous flat `--bg-surface` + `--border`-outline combo (itself a fix for an even earlier `opacity:.6` version) still read as too close to the modal's own background per a fresh Figma comparison on the email-code modal's resend button, nearly invisible against the card. `link` is 15px now (was 14) — read as slightly undersized on the deal-confirmation decline links. `.button-row` = two buttons side by side, `flex:1` each, 16 gap, secondary/cancel left. **Compact secondary (enabled) fills `--bg-elevated` now**, its own explicit override on top of the regular secondary's `--bg-raised` — a pixel-precise Figma export of just this one button pinned the fill at `#22262F` exactly, distinct from every other secondary button's darker `--bg-raised`. `loading` renders a shared `Spinner` component now (`src/components/Spinner`) instead of its own inline spinner span — see §2.4.
- **TextField / PasswordField** — plain label row, **no required asterisk** (removed — every field in the app is required anyway, the mark wasn't earning its place). Box: height 44, radius 12, fill `--bg-page`, border `--border` → `--border-strong` on focus, `--accent` on error, **12px horizontal input padding** (reduced from 16px — the text sat too far from the field edge). **`PasswordField`'s masked dots are the same 16px size as every other field's text now** — explicit instruction to match the email field exactly, reversing §2.2's own back-and-forth history (13px → 9px → "match Email's 16px" → smaller-dots-bigger-letters again). The `--masked` modifier class and its font-size/letter-spacing override are gone entirely — masked and revealed states now share one plain `.password-field__input` rule, no conditional class. `PasswordField`'s `labelHint` prop (a `ReactNode` rendered after the label, added for NewPassword's `HelpTip` "?" glyph) is gone too — `HelpTip` itself was deleted from the codebase once NewPassword's usage was removed and nothing else referenced it (see §6.2), leaving `labelHint` with no caller.
- **Logo** (new, `components/Logo`) — the real xRuby lockup, a 135×30 inline SVG supplied directly (not built up from CSS/text): a red rounded-square mark with a knocked-out "X" cutout, plus the "RUBY" wordmark in white. Replaces the old `<div>xRuby</div>` on SignIn and PasswordRecovery — two earlier attempts (plain red text, then a CSS-built badge+wordmark approximation) were both replaced once the actual brand SVG was supplied. Fixed colors baked into the SVG paths (white wordmark, `#C51625` mark) rather than tokens, since it's a literal asset, not a themed component.
- **AmountField** — label/available row, then a box (12px horizontal padding, matching TextField) with an optional embedded asset `Select`, the numeric input, and a "Макс" button. The old `transferVariant` (76px tall box, 28px input) was removed — the transfer modal's amount field now uses the same 44px sizing as every other field, per an explicit "make it match the standard fields" request. **The input groups thousands/millions live as you type** ("на всех экранах отделяем пробелом 3 разряда" turned out to still be missing from the one place numbers are actually *typed*, not just displayed — `formatAmount()` already covered every read-only display). The field stays a plain controlled `value`/`onChange` pair from the outside — `onChange` still fires with a clean `Number()`-parseable string, no spaces — the NBSP grouping is purely a display transform applied to what the `<input>` shows (`formatAmountInput()`/`sanitizeAmountInput()`, `lib/money.ts`; the same NBSP character `formatAmount()` uses, so a grouped `"1 234 567"` mid-typing and `"1 234 567.00"` once submitted look identical). `sanitizeAmountInput` also treats `,` as a decimal point, since Cyrillic keyboards' numeric layout produces a comma there — a small extra fix that fell out of needing sanitization anyway, not a separately-requested feature. Re-grouping a controlled input's displayed value on every keystroke moves the caret to the end by default (React has no way to know "the same digit" moved), so `AmountField` restores it itself: on each `onChange` it counts how many digits/decimal-points sat before the caret in the *old* string, then after the re-render finds the position after that many digits/decimal-points in the *new* (regrouped) string and calls `setSelectionRange` there (`useLayoutEffect`, so it lands before the browser paints) — typing or inserting a digit in the middle of a long number keeps the cursor sitting next to the digit you just typed, even as separator spaces shift around it.
- **Select** — `layout: 'asset' | 'method' | 'plain'`. Closed box (min-height **44px**, was 48 — brought in line with the Field spec) + click-open dropdown (rows min-height **36px**, was 48). Dropdown open/close now animates (`scaleY` + fade, 0.15s), the chevron rotates with a transition, and the box border-color transitions — previously all instant. There used to be a fourth layout, `'address'` (two lines: the full address, `word-break: break-all`, plus an optional `Метки: …` wallet-labels line) — it existed solely for WithdrawCrypto's saved-address dropdown, which was removed outright per an explicit "no dropdown, just an input field" instruction (§6.6); with that gone, `'address'` had no remaining caller and came out along with `SelectOption.labels`, `formatLabelsLine()`, and its CSS. `readOnly` (no chevron, unclickable) when there's ≤1 option — the readonly box used to zero out its own border (`border-color: transparent`), reading as flat/borderless against a Figma reference that wanted it to look like every other field; it now keeps the same 1px `--border` outline as the interactive state. `.select__label` lightened from `--text-muted` to `--text-body` for the same reason (readonly field labels read as too dim). A `trailingIcon` prop briefly existed (decorative `×` + chevron pair, only in the readonly state) for `RequisitesPanel`'s network field — removed again once that field became a real picker (see below) and the chevron took that slot back over; the prop had no other caller, so it and its CSS came out rather than staying as unused surface area. **The box's base fill is `transparent`, everywhere, no exceptions.** It's been through several rounds: `--bg-surface` → `--bg-raised` (per a WithdrawCrypto-specific Figma annotation) → transparent again once WithdrawFiat's own reference showed the opposite — at that point WithdrawCrypto and `RequisitesPanel` kept scoped `--bg-raised` overrides locally, since their own references still wanted a visible fill. An explicit later instruction ("все инпуты должны быть без заливки" — *all* inputs must be without fill) settled it as a hard global rule rather than a per-screen judgment call: both scoped overrides came out, and `CodeInput`'s cell fill (`--bg-raised`) came out the same way, gaining a permanent `--border` outline in its place (was `border: 1px solid transparent`, colored only on focus/invalid) so the cells still have a visible boundary at rest with no fill behind them. **`CodeInput` is the one exception, restored the very next round** — a proper full-resolution screenshot of the Google Authenticator modal (the "no fill" instruction had been given without one, and every previous attempt to show this modal specifically had arrived corrupted/truncated) showed its 6 cells clearly filled, distinguishable from the modal card behind them. Read narrowly: "no fill" was about form fields (text/select/amount), not this OTP-cell pattern — the border stays either way, the fill is back to `--bg-raised` for this component only.
- **PickerModal** — generic single-select list, presented as a **bottom sheet** (own scrim + card, not the centered `Modal`): drag handle, left-aligned title, plain full-width rows with the current value shown via a filled-row background (`--bg-raised`, radius 12) rather than a checkmark. Was originally built on the centered `Modal` with a checkmark-per-row and an explicit close `×`; rebuilt to the sheet presentation per a Figma annotation showing this exact bottom-sheet shape for Profile's Язык/Тема pickers. `languageModalTitle`/`themeModalTitle` shortened to just "Язык"/"Тема" (was "Выберите язык"/"Выберите тему") to match the sheet's plain title, not a question. Used by Profile's Язык/Тема rows (see §6.9); intentionally generic so any future "pick one of N" surface can reuse it instead of hand-rolling another dropdown.
- **Badge** — `success | neutral | count`. success/neutral: radius 20, `success` = green fill/text pair, `neutral` = `--bg-raised`. `count`: 34×24 box, radius 12, `--bg-surface` + border, white text — used by… nothing currently (deposit-account tab counters were removed per explicit request; component kept for future reuse).
- **Checkbox** — 20×20, radius 4, `--bg-page`/`--border-strong` unchecked, `--accent` fill + inline checkmark checked.
- **SegmentedControl** — plain-text tabs (no shared track pill), gap 8, item height 36 padding 0 14px. Active tab gets its own `--bg-raised` pill background + `--text-strong`; inactive is `--text-quiet` on nothing. Optional `count` badge per option (currently unused — see Home).
- **SettingRow** (new) — Profile's settings-panel row: 24px icon, label, optional right-aligned value + chevron, `danger` modifier (used by `Выход`). Tappable as a whole row (`onClick`), with a subtle active-state opacity/scale transition.
- **FeeBadge** — 38×20, `--bg-surface` fill, border.
- **StatCard** — 171×84-ish, radius 16, **`--bg-surface`** fill (was incorrectly `--bg-raised`, which made it invisible against a `--bg-surface` parent in one layout and is wrong regardless — the token table says stat cards are `--bg-surface`).
- **SummaryCard** — kv rows, 16 gap, bold "total" row, optional value+`tail` via a **single-line** value-group now (was a stacked two-line column — `flex-direction: column` → `row`, `white-space: nowrap` on the value, `flex-shrink: 0` on the group so the label wraps first if the row's tight, per explicit "write it on one line" feedback on the contract-address row specifically, the only caller of `tail`). **The crypto-withdrawal instance carries the "За вычетом комиссии сети" caption under its total row again** — an earlier round had removed it on the reasoning that the fee line item above the total already said this, but a fresh Figma reference showed it present, so it's back (`ru.withdraw.payoutCaption`, new key). Row alignment is `flex-start` now (was `baseline`) — with `baseline`, a two-line wrapped label (e.g. "Адрес контракта в блокчейне") and its value/tail column could align on the wrong line, reading as the value sitting one line lower than it should; `flex-start` keeps both columns' first lines level. The gap between the last regular row and the total row shrank to `8px` top padding (was 18) — read as too large a jump per the same pass.
- **Callout** — plain text block, no icon/title, 3 fills: `neutral` (`--bg-surface`, centered) / `warning` (`--panel-warning`) / `danger` (`--panel-error`).
- **WarningPanel** — **body text only now**, no icon and no title. Used to be icon (triangle) + title ("Внимание!") + body; per explicit feedback both came out and the body text just fills the panel, flush against its left padding once the icon (which sat before it in the flex row) is gone — "shift the text left" turned out to just be a consequence of removing the icon, not a separate alignment change. The `title` prop is gone from `WarningPanelProps` along with it (the component only takes `body` now) — its one call site (crypto withdrawal) stopped passing a title. Single `--panel-warning` fill (`#2B1509`), radius 12. Distinct from Callout.
- **Modal** — scrim centers the card both horizontally and vertically. Card width is a **per-instance prop** (`width`, default 400, still capped by `max-width: calc(100% - 32px)` for narrow screens) so one modal can be narrowed without affecting every other modal — added specifically because TransferModal needed 343 while the 2FA/email-code modals needed to stay responsive at their own width (see §6.11). Close button is a **bare 28px `×` glyph**, `--text-muted` (went 22px/`--text-quiet` → 28px/`--text-primary` on a compressed-screenshot annotation calling it too small and dim → `--text-muted` once an actual full-resolution screenshot of this exact modal showed the glyph reading closer to the body-text tone than to the bright white title — kept the 28px size from the middle round, only the color needed correcting). No fill/border either way, the earlier 28×28 `--bg-raised` circle read as small and drew attention as its own filled shape rather than a quiet corner control, so that idea stays dropped. The button's own box is still a full 44px tap target (pulled back with a negative margin so the larger hit area doesn't push the glyph off the card's corner). `body` padding is `0 24px 16px` with a 24px gap (was 8px/20px) and `bottom` padding is `28px 24px 24px` (top was 20px) — more air between the last body element and the footer button, most noticeable on the code-input modals where the button used to sit right under the digit cells; bumped again this round per the same annotation pass ("особенно между кнопкой и блоками с цифрами"). **`compactClose` prop** — an opt-in modifier used only by the three OTP/2FA modals (`VerificationModal`, `AuthenticatorModal`, `OtpConfirmModal`), per an explicit "make the × smaller in the OTP/2FA modals, and its top/right insets should match" instruction; `TransferModal`/`ConfirmDialog` don't pass it and keep the 28px default. `.modal__close--compact svg` drops to 20px; the box's own `margin` also changes (`-20px -12px -12px 0`, was `-12px -12px -12px 0` uniformly) because the base recipe's *effective* inset to the card's top edge and right edge were never actually equal (measured 16px top vs 12px right, from `.modal__top`'s asymmetric `16px 16px 16px 24px` padding interacting with flexbox centering of a negative-margin item) — the extra `-8px` on `margin-top` only, found empirically via `getBoundingClientRect()` rather than derived, brings both back to a measured-equal 16px for the compact variant specifically. The default (non-compact) 28px glyph's top/right insets stay whatever they were — this fix was scoped to the modals that asked for it, not applied as a global correction.
- **TabBar** — **no more hand-drawn home-indicator bar** (`.tab-bar__indicator`, the 134×5 white pill, is gone) — real devices already draw their own native home indicator, so ours stacked a second one right on top of it; per explicit feedback, the app draws no UI chrome for something the system already renders. `position: absolute; bottom: 0` inside `.app-shell`, 88px + safe-area, radius 16 top corners, **no top border** (removed — a Figma export of this element showed the tab bar's top edge flush against the drop shadow with no hairline). Background is `--bg-page`, not `--bg-surface` — darker than the app's surface color, per a Figma annotation; the three tab icons (`icons.tsx`) are solid/filled shapes (`fill="currentColor"`), not outline strokes, matching that same reference. `DealsIcon`'s inner "text line" cutouts are drawn in `fill="var(--bg-page)"` specifically so they read as negative space against the tab bar's own background rather than needing a separate color token. It never scrolls: `html`/`body` are `overflow: hidden; overscroll-behavior: none` (`src/index.css`), and `.tab-bar`/`.app-header` carry `overscroll-behavior: none` directly too, so a touch-drag starting outside `.app-content`'s own scroll area — e.g. directly on the tab bar — can't rubber-band the document itself. That mattered because the tab bar is only pinned relative to `.app-shell`, not the viewport; if the document could scroll/bounce, the "fixed" tab bar visibly dragged along with it. `.app-content` keeps its own `overscroll-behavior: contain` on top of this so its internal scroll never chains to a parent that no longer has anywhere to go. **`html`/`body` are deliberately not `position: fixed`** — an earlier version of this fix used that too, as a belt-and-braces measure from before `overscroll-behavior` had solid WebView support, and it caused a real regression: pinning the document out of normal flow fights the OS's own keyboard-avoidance scrolling, so focusing any text field made the page — and the input/cursor with it — visibly jump or drift as the on-screen keyboard opened, and the same dynamic made the OTP/2FA modal look shifted while a field inside it was focused. `overscroll-behavior: none` alone already stops the bounce without taking over the document's layout, so it's the only mechanism now.
- **CodeInput** — 6 cells, `--bg-raised` fill (briefly removed for one round under the app-wide "no input fills" rule, restored once an accurate screenshot of the Google Authenticator modal showed this component specifically keeps a fill — see the `Select` entry above), plus a real always-on `--border` outline (was `border: 1px solid transparent`, colored only on focus/invalid — added when the fill briefly came out so the cells kept a visible boundary, and kept since), radius 8, digit `clamp(18px, 6vw, 24px)`. **Responsive now**: cells are `flex:1` with `max-width:44px` and `aspect-ratio:1`, gap `min(8px, 2vw)` — on a 400px-wide modal card they render at the original 44×44, but on a narrow phone (~340px viewport, ~280px available body width) they shrink to fit instead of overflowing the modal. The overflow was the "кривая модалка" bug reported against the 2FA/email-code modals: six fixed 44px cells plus gaps needed ~304px, more than the modal body had on a narrow screen.
- **TableRow** — see §2.3.
- **CurrencyIcon** — per-ticker brand mark, each its own inline-SVG component rather than one generic shape: KGS/USD are flag icons (KGS: red field + 20-ray gold sun; USD: red/white stripes + a 3×3 star grid in the canton), BTC/USDT/USDC/ETH/RUB are colored-circle marks approximating the real brand glyphs (Tether's T-through-ring, USDC's circular double-arrow + $, Ethereum's stacked-diamond, BTC/RUB via a bold `<text>` currency glyph rather than a hand-transcribed path, to avoid a mistranscribed bezier rendering as a malformed shape). Any ticker without a dedicated icon falls back to a neutral grey `SymbolCoinIcon` (first letter) — deliberately generic, not defaulting to any one brand color.
- **StatusChip** — tone is the literal `DealStatus` union value (`.status-chip--RATE_ACTIVE` etc.) plus a `success` tone; sizes `sm` (**16px/radius 8, font 10px** — shrunk from 18/radius 9/font 12 per a Figma annotation calling the list-row badges too large) and `lg` (24/radius 12, font 12px, deal-detail header — kept at its previous size since only the list-row badge was called out).
- **StatusHero** — icon (50px) + title + subtitle + action, padding-top 16/bottom 16, gradient per status (see §2.1). `StatusHeroTone` = `success | danger | running | stale | pending`.
- **DealRow** — see §2.3.
- **EmptyState** — centered tray-illustration SVG (70×60) + caption, used by Home's empty recent-deals and Deals' empty-filter state.
- **DocumentRow** — 16px download-glyph icon, locked to `16×16` via CSS on the `<svg>` itself (not left to the element's own `width`/`height` attributes) so the enabled and disabled rows render it at an identical size — they'd drift on some platforms otherwise.
- **RequisitesPanel** — wraps itself in `Panel fill="surface" radius="16px"` with its own inline `<h2>` heading instead of the old `Panel heading=` prop (that prop was removed — `Panel` is a pure layout primitive now, see §2.3). Crypto variant: the QR plate now carries the brand mark knocked out at its centre (40px, `--brand-mark` fill, white "X", white border) — the code generates at `errorCorrectionLevel: 'H'` specifically so it stays scannable under the mark. **The asset and network selects now share the same `--bg-raised` fill** (was `--bg-raised`/`--bg-page` — two different fills, meant as a "pick vs. read-only" distinction, but a Figma annotation showed both fields reading as the same lighter tone with a border) with `16px margin-top` between them (was effectively 0 — the two field wrappers had no spacing rule of their own and sat flush against each other). The "Или скопируйте адрес" caption gained flanking divider lines (`::before`/`::after`, 1px `--border`) — the "Отсканируйте QR-код" caption above it stays plain text, only the "or" caption between the QR and the address row got dividers, matching the reference. The address text is weight 600 now (was unweighted/400) and its own local `CopyIcon` was redrawn to match the two-overlapping-squares glyph used everywhere else (`Profile`, etc.) — it had drifted out of sync with that redesign since it's a separate copy of the icon local to this file, not a shared component. **The address is no longer middle-truncated** (`truncateMiddle` dropped) — it renders in full, wrapping (`word-break: break-all`) rather than eliding, per explicit feedback that a shortened deposit address read as untrustworthy to copy from. The row's `justify-content: space-between` came out too: with the text now `flex: 1` it fills the row itself, so the copy button naturally lands right after the (possibly two-line) address instead of being pinned to the row's far edge regardless of text length.

**The network field is a real picker again, not a fixed display.** `CryptoRequisites` gained an optional `networks?: { network, address }[]` (`api/types.ts`) — when present, the network `Select` gets one option per entry instead of a single hardcoded one, and picking a different network updates both the shown address and the QR code (local `selectedNetwork` state, `current = networks.find(...)`). Falls back to the single `network`/`address` pair — and therefore back to `Select`'s own `readOnly` single-option behavior — when the backend hasn't sent alternatives yet. `MOCK_REQUISITES_CRYPTO` now carries both a TRON and an Ethereum entry for USDT so this is exercisable in mock mode. This reverses the `trailingIcon`/fixed-display version from the round just before it — that version assumed the network was fixed per deposit address (true for a single real address, but the actual requirement was to let the user choose which network to deposit over, each with its own address).
- **BalanceBlock**, **KeyValueRow**, **FilterChips**, **CompanyChip**, **Toast** — unchanged in shape, retokenized onto the current palette. (`HelpTip` was on this list too until it was deleted outright — see the `TextField`/`PasswordField` entry above. `Skeleton` was too, until its shimmer rework — see §2.4.)
- **ConfirmDialog** — still exists (used for the deal-decline and cancel-request confirmations in DealDetail) but Profile's sign-out no longer goes through it, see §6.9.
- **BlockingState** (OtcUnavailable's shell) — logo + centered 80×80 shield illustration + title/body/action/caption.
- **AuthenticatorModal** — 6-digit `CodeInput`, `compactClose` (§3). Used to also show a "Возникли проблемы с синхронизацией?" link below the code cells (a stub, opened the shared "Раздел в разработке" toast, not a real re-sync flow) — briefly left-aligned to override `Button`'s centered `link` variant once a screenshot showed it shouldn't be centered, then removed outright the next round per explicit instruction ("убери строчку Возникли проблемы с синхронизацией"). Its `Button`/`useToastStore` imports and the `ru.authenticator.syncHelpAction` string went with it — nothing else referenced them. Reuses the same mock `verifyCode` as email verification (any 6 digits except `000000`).
- **TwoFactorGate** — the shared second-factor entry point. Renders `AuthenticatorModal` when `authenticatorEnabled` is true, `VerificationModal` (email code) otherwise. Used identically by sign-in, crypto withdrawal, and fiat withdrawal — see §5.
- **QrScannerModal** — full-screen camera overlay (`getUserMedia` + `jsQR`), used to scan a wallet address into the crypto-withdrawal manual-address field. Parses `bitcoin:`/`ethereum:`/`tron:`-style URI payloads down to the bare address. Permission-denied state shows a retry button.
- **TransferModal** — global modal (`store/transferModal.ts`), opened from any withdrawal screen's amount-field transfer glyph. Rebuilt this round against its Figma frame — full geometry and behaviour in **§6.11**, that section is the source of truth for this surface.

## 4. Navigation

| Path | Screen | Tab bar | Header |
|---|---|---|---|
| `/login` | SignIn (business) — **default** | no | close |
| `/login/personal` | SignIn (personal) | no | close |
| `/login/business` | redirect → `/login` | — | — |
| `/forgot` | PasswordRecovery | no | back |
| `/reset-password` | NewPassword | no | back |
| `/otc-unavailable` | OtcUnavailable | no | close |
| `/home` | Home | yes | brand ("XRuby") |
| `/deals` | Deals | yes | close |
| `/deals/:id` | DealDetail | no | back |
| `/profile` | Profile | yes | brand |
| `/withdraw/crypto` | WithdrawCrypto | no | back |
| `/withdraw/fiat` | WithdrawFiat | no | back |
| `/withdraw/fiat/requisites` | WithdrawRequisites | no | back |

`/login` (business) is the default sign-in route, `/login/personal` the alternate — a deliberate, standing deviation from generic nav conventions, made explicitly at the user's request; do not "fix" this back to a personal default.

**Both are `headerVariant: 'close'`, never `'back'`.** `/login/personal` used to be `'back'`, so toggling personal ↔ business made the header (and the native BackButton) flicker between close and back depending on which variant you landed on — wrong regardless of direction, since business and personal are two faces of one entry screen, not a parent/child pair with real navigation depth between them. Combined with the `replace`-navigation above, the two variants now behave as one screen for every purpose that matters (header, native back button, history) even though they're still two route paths internally.

**The personal/business switch links navigate with `{ replace: true }`, not a push.** They previously pushed a new history entry each time, so toggling personal → business → personal a few times left a long chain of login-variant entries in the browser/Telegram back-stack — pressing back would step through login variants instead of leaving the auth flow. Any "switch between sibling views of the same screen" link (as opposed to "go deeper") should follow this pattern: `replace`, not `push`.

AppHeader only renders when `isRealTelegram` is false (or `VITE_FORCE_INAPP_HEADER=true`) — inside a real, correctly-detected Telegram launch, Telegram's own native BackButton is used instead. The clearance `.app-content` reserves for it is measured live off the pill's rendered height (`AppHeader.tsx`'s `useHeaderClearance`, via `ResizeObserver`) plus a small fixed gap — tightened this round from a 28px gap/100px fallback down to a **2px gap / 48px fallback**, so the screen title sits almost immediately under the pill instead of leaving a visibly large empty band at the top.

**The native BackButton (`telegram/backButton.ts`'s `useAppBackButton`) only shows for `headerVariant: 'back'` routes, never `'close'`.** It used to show for both, which meant `/login` and `/otc-unavailable` — genuine entry points, nothing to go back to, even right after signing out — got Telegram's native back chevron anyway; tapping it did `navigate(-1)`, stepping back into whatever screen preceded the entry point (the session guards on those screens redirect straight back to `/login`, so this wasn't exploitable, but it read as a real navigation escape and shouldn't have been offered at all). `'close'` routes now leave the native BackButton hidden so Telegram's own default close affordance shows instead — there is no native "close" button we can render ourselves, only show/hide on the back one, so `'close'` semantics are "hide ours and let the platform's default take over," not a different button we draw.

**In real Telegram (no pill of our own), the fallback clearance is a live `calc()` expression, not a one-time computed number.** This fixed a real bug: that branch is keyed on `active`, which never changes across in-app navigation once we're in real Telegram, so its effect only ever runs once per cold load — and on cold load Telegram hasn't necessarily reported its safe-area insets yet. Baking `getComputedStyle(...)` into a plain px number at that moment could snapshot 0, and every screen navigated to afterward inherited that stale value (only a full reload re-ran the app late enough for the real inset to be available, which is why "content jams under the status bar on navigation, but is fine after reload" was the reported symptom). Writing `--header-clearance` as `calc(48px + max(var(--tg-viewport-safe-area-inset-top, 0px), var(--tg-viewport-content-safe-area-inset-top, 0px)))` instead keeps it correct forever — the browser re-resolves the `var()` references live whenever Telegram updates them, no JS re-run required.

**Every screen's own top padding is now a uniform 8px**, on top of that shared header clearance. It had drifted apart per-screen (Deals 4px, DealDetail 0px, Home/Profile/Withdrawals 16–20px, auth screens 24px) as each was built independently, which read as an inconsistent gap under the header when moving between them — measured now, every screen sits exactly 10px below the pill's bottom edge (2px clearance gap + 8px screen padding), no exceptions. Keep new screens on this same 8px top value rather than picking a fresh one.

## 5. Auth & second factor

Rebuilt this round against `api-integration.md` — see the top-of-file note for why (an incompatible earlier Telegram-binding architecture was fully replaced, not extended).

**A Telegram-identity-binding layer now sits on top of the email+password+OTP flow, backend-confirmed (not read off a Swagger file like everything else in this doc) — the third auth architecture this app has had.** Round 1 was a Telegram-binding `session.start`/`login`/`login-confirm` flow (`miniapp-auth-integration-spec.md`, deleted). Round 2 replaced it wholesale with `api-integration.md` §2's plain email+password+OTP-every-launch model, where `initData` was explicitly *not* an auth mechanism — just a one-shot header sent once after sign-in to bind `telegram_id` for push (`x-telegram-init-data`, `markInitDataBindPending()`). **Round 3, this one, replaces that one-shot header with a real binding flow** — conceptually closer to round 1, but a different concrete contract, given directly by the backend team rather than inferred from Swagger:

- New `real/miniAppAuth.ts`'s `authenticateMiniApp(initData, bearerToken?)` — `POST /telegram-mini-apps/{clientId}/authenticate` (`auth` service), `clientId` from `VITE_TELEGRAM_MINI_APP_CLIENT_ID` (env, never hardcoded). Two calling modes: no bearer = silent re-entry (binding already exists, returns a mini-app access token straight away); `Authorization: Bearer <platform JWT>` = creates the binding and returns the same shape. The platform JWT here is the OTP flow's own `/oauth2/token` result — `real/auth.ts`'s `signInConfirmOtp` now returns `{platformAccessToken}` instead of saving it as a session, since it's spent exactly once (as that Bearer) and discarded; the token that actually becomes the session is whatever `authenticateMiniApp` hands back. This orchestration — OTP confirm → spend the platform JWT → save the mini-app token — lives in `api/index.ts`'s `signInConfirmOtp` wrapper, not `SignIn.tsx` or `real/auth.ts`, specifically so both modes keep returning one `Session` shape and no screen needs a `USE_REAL_API` branch for it.
- **Every real-mode cold boot (`index.tsx`) tries `authenticateMiniApp(freshInitData)` silently, before ever rendering the login screen.** If this Telegram identity is already bound, a session appears with zero login/password/2FA — `SignIn.tsx` only renders because that came back empty-handed (genuinely unbound, or a transient failure; both are read the same way, see below). This is a real, awaited network call before `root.render(...)`, unlike round 2's fully synchronous boot — the app has no way to know if a launch is "first ever" or "the hundredth" without asking the backend first.
- **`tokenStore.ts` lost its refresh-token model entirely** — the mini-app OAuth client the backend registered explicitly has no refresh token. What used to be `refreshTokens()` calling `POST /oauth2/token` with a stored `refresh_token` is now a pluggable `reauthenticate` callback, wired once at boot (`setReauthenticator`) to `() => authenticateMiniApp(getFreshInitData())` — a lapsed access token gets replaced by silently re-running the exact same silent-boot call, not a grant exchange. No more `localStorage` refresh-token persistence either: the durable credential is the backend-side binding, discoverable fresh via `initData` on any device, any relaunch — not a secret this app has to keep safe locally. `sessionStorage`-restoring the access token itself is kept as a same-tab-reload optimization only (skips a network round trip on a plain page refresh).
- **`clientType` on a silent re-entry is a persisted client-side guess, not backend truth** — `lib/lastClientType.ts`, `localStorage`, defaults `UL`. Nothing in the profile response or the authenticate call carries an account-type field (`real/profile.ts`'s `getUser` already ignored its own `clientType` param for exactly this reason, pre-dating this round), so `clientType` has always been a display-only label in real mode, not a validated fact — this just persists the label across the moment (first bind) where it *was* known (whichever login screen variant the user actually completed) so silent re-entries later have something reasonable to show.
- **Two things are explicitly unconfirmed, isolated to one place each so a wrong guess is a one-line fix**: the success response's field names (assumed camelCase `accessToken`/`expiresIn`, `real/miniAppAuth.ts`) and the exact signal for "not bound yet" (any non-2xx here is read that way, rather than switched on a specific status/code — none was given). A **new, unresolved product question**: tapping "Выход" only clears local state (there's no documented revoke/unbind endpoint for this token) — since the binding itself persists server-side regardless, the very next boot's silent re-entry would immediately undo an explicit sign-out. Flagged in `real/auth.ts`'s `signOut` comment, not silently resolved either way.
- `x-telegram-init-data` and `markInitDataBindPending()` are gone outright, not just unused — deleted from `api/http.ts` and its one caller in `SignIn.tsx`, per explicit instruction.
- Social sign-in's `exchangeSocialCode` (already disabled, see below) used to save its own tokens into `tokenStore` directly — that call is gone too (the function it called no longer exists in the new token model); left returning the raw exchange result unsaved, flagged as unreconciled with this flow, not silently made to look like it still works.
- **Not verified end-to-end** — no Telegram context or live dev backend was reachable from this environment. Verified via `tsc`/lint/build plus a full mock-mode Playwright pass proving the (unaffected, `USE_REAL_API`-gated) mock boot path still renders and signs in cleanly. The real path needs an actual run inside Telegram against `https://api.dev.xruby.kg` to confirm.

**Two genuinely different "second factor" models now coexist, each with its own component** (unrelated to the binding layer above — this is about the standard login form's own OTP step, which still runs in full on a first-time bind):

- **Sign-in and password recovery — one second factor per account, never both at once.** Originally built as "always email/SMS OTP, *plus* an authenticator code when the account has one enabled" (a literal reading of §2.1's `twoFA` boolean) — **reversed by explicit product decision**: an account has exactly one method configured, and the modal should show that one field, mirroring how the withdrawal-confirmation `TwoFactorGate` trio already picks a single modal off an operation's `source` rather than stacking both. `OtpConfirmModal` (`src/screens/OtpConfirmModal/`, renamed and repurposed from the old Telegram-binding-era `LoginConfirmModal` it was adapted from — same shape, new internals) now renders exactly one `CodeInput`, titled/copy-branched by a new `source: 'email' | 'authenticator'` (`AuthOtpSource`, `api/types.ts`) — email gets the existing "Отправили код на {email}" + resend-with-countdown footer; authenticator gets `AuthenticatorModal`'s own title/body copy and no resend footer at all (an authenticator code isn't "sent" anywhere to resend). `SignIn.tsx`: `signInRequestOtp(email, password, clientType)` → `{transactionId, source}` → opens `OtpConfirmModal` → `signInConfirmOtp({transactionId, otp, email, clientType})` → `Session` — no more separate `twoFaCode` field anywhere, one `otp` field regardless of which source it came from. `PasswordRecovery.tsx` follows the identical shape. Mock fixtures: the business account (`UL`, `authenticatorEnabled: true`) gets `source: 'authenticator'`; the personal account (`FL`) gets `source: 'email'` — both branches exercisable without a dev switch, unchanged from before. Real mode's exact field name for this on `/oauth2/otp`'s response is unconfirmed (assumed to be `source`, matching the withdrawal OTP contract's own field of the same name and purpose) — isolated to `real/auth.ts`'s `signInRequestOtp`, one place to correct if the backend calls it something else. This incidentally fixed a latent bug in `PasswordRecovery.tsx`: its `handleOtpSubmit` only ever forwarded `{transactionId, otp}` to `recoveryConfirmOtp` (which never took a second code parameter), so a `twoFaCode` typed into the old always-both-fields modal was silently discarded for recovery specifically — moot now that there's only ever one field to fill.
- **Withdrawal confirmation — Google Authenticator *or* the email code, never both**, unchanged this round: `TwoFactorGate` picks one of `AuthenticatorModal`/`VerificationModal` based on `User.authenticatorEnabled`, both still call the old generic `sendVerificationCode`/`verifyCode` pair (now explicitly scoped to this use only — see `real/auth.ts`'s own comment). This is deliberately *not* yet wired to `api-integration.md` §5.3's real `issue-otp`/`confirm` endpoints — that's a Withdrawals-step task, not part of this round's Auth rewrite.
- Social sign-in (Google/Apple) skips both models — mock stays an instant fake session (`signInSocial`); real opens an external browser (`startSocialSignIn`) and completes best-effort on relaunch if the redirect is captured (`exchangeSocialCode`, checked once at boot in `index.tsx`) — §2.2 flags this as best-effort for MVP (question B3), not blocking. **Temporarily hidden from `SignIn.tsx` entirely** (`SOCIAL_AUTH_ENABLED = false`, a local constant, both buttons and their wrapping row) — no real contract for it exists yet and the test plan's own B3 question asks whether social sign-in should just stay off for the mini app. All the wiring above is untouched underneath; flip the flag back once the API lands.
- Both `CodeInput`-based modal families share the same responsive `CodeInput` (§3).

## 6. Screens

### 6.1 SignIn (`/login`, `/login/personal`)
`Logo`, title ("Вход в бизнес-аккаунт" / "Вход в личный аккаунт"), email, password (eye toggle), "Забыли пароль?" link, submit button, Google/Apple social buttons, footer switch-account link ("Войти в бизнес-аккаунт" / "Войти в личный аккаунт", `replace`-navigated per §4). 2FA per §5. Content inset 20px (all auth screens; every other screen is 16px, see §2.3).

Top padding is **24px** here (not the app-wide 8px, see §4) — auth screens carry a logo above the title and needed more air around it; `.logo` also has its own **16px** `margin-bottom` (was 8) on top of the normal 16px inter-element gap, so logo→title reads as a full 32px against a design reference, not 24. Title weight is **500** (went 700 → 600 → 500 across two rounds, each still reading heavier than the reference). `Забыли пароль?` is **16px** (700 → 15 → 16, same iterative reasoning). The Apple social button's icon is the standard flat Apple-logo silhouette path (`simple-icons`' `apple` glyph) — an earlier hand-drawn approximation didn't read as recognizably Apple. The footer switch-account link's trailing glyph is a proper **arrow SVG** in `--text-primary` now, not the plain `›` character in `--text-quiet` it used to be — the reference showed a straight right-arrow, lighter than our chip-grey chevron. `PersonIcon`/`BuildingIcon` (the leading glyph on that same link) were already outline SVGs, not the filled icons an earlier round's screenshot seemed to show — that read turned out to be a stale/cached build, not the actual source.

### 6.2 PasswordRecovery (`/forgot`) / NewPassword (`/reset-password`)
Flow unchanged: email → `sendVerificationCode` → code modal → new-password form.

**NewPassword was rebuilt to match its design reference** — it previously had no `Logo`, no password-generator, and no strength indicator at all (title was also "Новый пароль", not the reference's "Восстановление пароля"). Now: `Logo` → title → `Новый пароль` field (16px placeholder "Введите новый пароль" — no longer has a `HelpTip` "?" glyph next to the label, removed per an explicit "remove the info-field question marks" pass, which is what took `HelpTip` out of the codebase entirely) → a `Сгенерировать пароль`/strength row → `Подтверждение пароля` field ("Подтвердите пароль") → `Продолжить`.

`Сгенерировать пароль` fills **both** fields with the same generated 12-character password (4 required classes — upper/lower/digit/symbol — plus 8 more, shuffled), copies it, and toasts `Пароль скопирован` — matching the "generate once, both fields get it" behavior the reference implies. The strength indicator (`computeStrength` in `NewPassword.tsx`) is two states, not three: **Слабый** (`--accent` red dot) below 8 chars or under 3 character classes, **Надежный** (`--warning-text` amber dot — that token was sitting unused since Profile's security-level row was removed, see §6.9) otherwise; hidden entirely while the field is empty. Only two tiers because that's what the reference shows — no evidence of a third ("Средний") state to build against.

### 6.3 Home (`/home`)
Full-bleed panel model, see §2.3:
1. Hero: "OTC" title/subtitle, desk-hours caption, `CompanyChip` (client name), two `StatCard`s (active deals count, 30-day volume). The title/subtitle/desk-hours trio is wrapped in its own `.home__hero-text` flex column with an **8px** gap — tighter than the 20px gap from that trio down to the company chip/stat cards below it, per a Figma annotation calling out the top block specifically.
2. "Недавние заявки": up to 2 `DealRow`s (no per-status highlight, see §2.3), or `EmptyState` if none.
3. "Депозитный аккаунт": `SegmentedControl` (Криптовалюта/Фиат, no count badges — removed per an earlier request), then `TableRow` per positive-balance asset, "Активы с нулевым балансом скрыты" caption when any are hidden. Radius is **`24px 24px 0 0`**, not a uniform 24 — it's the last panel on the screen, so rounding the bottom corners too just let the `--bg-raised` frame color peek through as two small grey curves under an otherwise straight edge.

### 6.4 Deals (`/deals`)
No panel, **no screen title any more** (the "Заявки" `<h1>` was removed per a Figma annotation — the filter chips now sit right under the header) — `FilterChips` (Все/Активные/Исполненные/Отклонённые, 36px pills, horizontal-scroll, last chip deliberately clipped, **6px gap** between pills — went 8 → 12 → 6 across rounds, the middle value read as too loose) sit directly on the bg-page frame at 16px inset, then a flat list of full-bleed `DealRow`s (loading skeletons, error+retry, or `EmptyState` — no deals at all, or none matching the filter, with a "Показать все" reset link). The active chip fills `--bg-elevated` with a `--text-strong` label — it briefly went to a light fill (`--text-primary` background, `--text-on-light` label) in an earlier round per a compressed screenshot reference, but a later pixel-precise Figma export of the chip itself (`#22262F` fill, `#ECECED` text — exactly `--bg-elevated`/`--text-strong`) showed the dark-pill treatment was actually correct; the light-fill round is superseded.

### 6.5 DealDetail (`/deals/:id`)
No panel — header (id + `StatusChip` lg + direction) sits directly on bg-page at 16px inset. `.deal-detail__id` is weight **600** (was 700, read as too heavy) and `.deal-detail__direction` is **16px `--text-muted`** (was 14px `--text-quiet`, read as too small/dim) — both per a Figma annotation pass. Three-way body split on status:
- **`RATE_ACTIVE` → QuoteCard** (own `--bg-surface` card): rate (**28/36**, weight 600 — was 36/44, shrunk along with the card's other text per the same pass: the "Заявка подготовлена…" and "Курс для вас" lines are now 14px, were 16), a live countdown (`m:ss сек`, client-`setInterval`, re-synced on `visibilitychange`/focus, never trusts elapsed device time), accept (`confirmDeal` → `RUNNING`) / decline. Flips to `RATE_STALE` at zero via the mock's `expireQuote(id)`. No balance math here. The decline link sits `margin-top: 12px` below the accent button now — it used to inherit only the card's base `4px` flex-gap, reading as cramped against "Подтвердить сделку".
- **`AWAITING_FUNDS` → ConfirmationBody (hold confirmation)**: `BalanceBlock` (amount now **28/36**, was 36/44; ticker suffix 14px, was 16; its transfer-icon button is 28×28, was 20×20 — read as too small to tap confidently; the glyph itself was swapped again to a pixel-exact export, a 20×16 double-arrow swap icon rather than the earlier hand-drawn loop-arrows attempt — `currentColor`-filled, still resolves to `--accent`) → `Callout` (14px body text, was 16; 4 balance-vs-deal-amount cases: sufficient/short1/short/below-min — `short1`/`short` are `warning`, `belowmin` is `danger`) → confirm/decline (same 12px-gap fix as the quote card, plus the shared `.button--link` bumped to 15px, was 14, per the same pass) → `RequisitesPanel` (own `--bg-surface` card). Its chip/list label is **"Пополните баланс"** everywhere now — it used to read "Ожидает пополнения" in the list and "Пополните баланс" in the detail header, two different labels for one status; unified on the detail's wording.
- Everything else → **StatusHeroBody**: `RATE_PENDING` (pending tone), `RATE_STALE`, `RUNNING`, `DONE`, `DECLINED` — icon/title/subtitle/action per status. `StatusHero`'s icons shrank from 50px to 40px with thinner strokes (spinner ring 4px→3px) per the same annotation pass calling the ring "too big." The terminal-state glyphs (check/cross/hourglass) were rebuilt as a shared `Medallion` — a light radial-gradient grey disc with a thin rim highlight — replacing the old flat dark `--status-*-fill` circle, matching the reference's "coin" look; only the glyph inside stays tone-colored (`--status-done-text` etc.). `.status-hero--stale`'s gradient was a bare `background-image: none` — a real bug, since `--hero-top-stale` existed as a token but was never wired up; it now uses the same top-tinted gradient formula as every other tone. `RATE_PENDING`'s subtitle carries an explicit `\n` (`.status-hero__subtitle` is `white-space: pre-line`) so "В рабочее время" and "(пн–пт 10:00–18:00 мск)" render on two lines, not one wrapped run. **`DONE`'s "История операций" action link is gone** — there is no transaction-history surface anywhere in this mini app (never was), so a link that only ever opened the generic "in development" stub had nothing real to point at; removed rather than left as a permanent stub.

`Детали` and `Документы` are plain heading + rows directly on the page background — **not** cards (see §2.3), section-title now **16px/24** (was 18/28) with a **6px** gap to the first row (was 12), also per the annotation pass. **`Детали` always shows the full 5-row set now** — `showsFullDetailsRows()` used to hide direction/dates/amounts for `RATE_PENDING`/`RATE_ACTIVE`/`RATE_STALE`, but a fresh Figma comparison showed Дата создания/Направление present for all three, so the function just always returns `true` now; `Курс` still reads "Уточняется" for `RATE_PENDING`/`RATE_STALE` specifically (that logic is separate, in `DealDetail.tsx` itself, unchanged). "Документы" availability keyed by `lib/dealStatus.ts`'s `getDocumentAvailability()` — **`DECLINED` now sets `showCaption: true`** (was `false`, a real bug: it silently dropped the "Станет доступен позже" captions under DECLINED's disabled document rows, the only status where that happened). `DocumentRow`'s own name→caption gap is `0` now (was 4px) per "no gap" feedback on that specific spacing.

### 6.6 WithdrawCrypto (`/withdraw/crypto`)
**No screen title any more** ("Вывод криптовалюты" was removed per a Figma annotation — the screen now opens straight on "Выберите актив") — asset `Select` (asset layout) → address section → network `Select` (non-BTC only) → amount `AmountField` (with transfer-modal glyph, no `flexShrink`-caused gap issue any more — its icon-to-label margin dropped to 0) → `SummaryCard` (min/limit/contract-tail/fee/payout, with the "За вычетом комиссии сети" caption restored, see §3) → `WarningPanel`. Section-title headings (`Выберите актив`/`Выберите адрес и сеть`/`Сумма вывода`) dropped from weight 700 to 600, read as too heavy per the same pass. `AmountField`'s balance line no longer repeats the ticker (`Баланс: 56 889.65`, was `Баланс: 56 889.65 USDT` — the ticker's already on the field's own asset icon/label right above it), and its own label is "Вывести" now (was "Отправить" — shared `ru.withdraw.sendLabel`, also used by WithdrawFiat, both screens' references showed "Вывести"). Its transfer-icon glyph is the same pixel-exact double-arrow swap icon as `BalanceBlock`'s now (§6.5) — it had been left on an earlier hand-drawn loop-arrows attempt when `BalanceBlock`'s got swapped a couple of rounds back, per an explicit "make it the same as on the top-up-balance status" instruction. The submit button's sticky footer gained more top padding (20px, was 12) for breathing room above it. **This screen's `Select` boxes no longer keep a scoped `--bg-raised` fill** — they briefly did (a round where WithdrawCrypto's own reference showed bordered grey boxes while WithdrawFiat's showed flat/borderless ones, so each screen got its own scoped choice), but the later blanket "все инпуты без заливки" instruction (§3, `Select` entry) removed that scoped override along with everything else; this screen's fields are transparent like every other `Select` now. The "Адрес кошелька" label sits closer to the field below it now too (`margin-bottom: -6px` on the label, clawing back some of `.withdraw-crypto__scroll`'s 12px child gap) — that gap was the screen's shared vertical rhythm value doing double duty here where a tighter, more clearly-"this label belongs to this field" spacing was wanted instead.

**The saved-address dropdown is gone entirely — this screen is now always a plain `TextField`.** A prior round added a merchant-level `CryptoWithdrawOptions.addressEntryMode: 'dropdown' | 'manual'` flag plus a saved-address `Select` (address layout, listing saved addresses + "Новый адрес"), mirroring how WithdrawRequisites' fiat flow offers saved requisites. Explicit instruction reversed this: "не должно быть дропдауна... просто поле ввода адреса кошелька" — unlike fiat withdrawal's bank requisites, crypto withdrawal addresses never get a saved-address picker at all, full stop, regardless of what the backend could theoretically send. Removed: `addressEntryMode` off `CryptoWithdrawOptions`, the whole `SavedAddress` type and its `addresses` field/fixtures, `addressEntryModeOverride()` dev switch, the `Select` `'address'` layout (and its `labels`/wallet-metadata-line support — nothing else used it), and the `NEW_ADDRESS_OPTION`/`newAddressOption`/`addressLabelsPrefix`/`addressHelpText` constants and i18n strings. `address` is now just `manualAddress.trim()`. `QrScannerModal` still opens from the field's suffix button. (WithdrawRequisites' *own* "Выберите реквизиты" dropdown, §6.8, is a different flow and unchanged — it already correctly only renders when `getSavedRequisites()` returns entries compatible with the chosen transfer type, i.e. exactly the "only show it if the backend actually returned saved requisites" rule the fiat side should follow.)

Submit: validate address + amount against the live `quote` → `issueWithdrawOtp(quote.transactionId)` picks the modal via its `source` → `TwoFactorGate` → `confirmWithdrawOtp(quote.transactionId, code, { address })` (see §8's crypto-withdrawal entry for the full real-API shape).

### 6.7 WithdrawFiat (`/withdraw/fiat`)
**No screen title** ("Вывод средств" removed per a Figma annotation, same reasoning as WithdrawCrypto's title). Currency + method `Select`s, amount field, summary (min/limit/entered amount/fee/total debit) — a **static** preview computed from the chosen `WithdrawMethod`'s own `minimalAmount`/`maximumAmount`/`commissionPercent`/`commissionFixed`, not a live quote (the live quote only fires once on `WithdrawRequisites`, see §6.8 — a bank-requisites form has too many fields for a debounced live quote to make sense the way the crypto address field does). No submission here — "Продолжить" navigates to `WithdrawRequisites` carrying `{ ticker, paymentType, operationOption, amount }` via route state (was `{ ticker, methodId, amount }` — `WithdrawMethod` no longer has a synthetic `id`, real methods are addressed by their own `(paymentType, operationOption)` pair, per `api-integration.md` §5.1).

The method `Select` (`layout="method"`) now carries a generic `MethodIcon` (a small flat "bank" glyph in an `--accent`-filled circle, local to this screen — there's no per-bank logo asset, so every method gets the same neutral mark rather than none at all, which is what shipped before: `MOCK_WITHDRAW_FIAT_OPTIONS`' methods never had an `icon`, so the row silently rendered without one). Its fee badge sits directly next to the method name now, inside `row-left` — the `method` row layout used to put it on the far right via the row's own `justify-content: space-between`, reading as "unrelated to the label it's on the far side of" per the annotation ("лейбл должен быть приклеен к тексту"). Unlike WithdrawCrypto, these `Select` boxes use the (now-transparent) base fill — no scoped override — matching a Figma reference that showed them flat/borderless.

### 6.8 WithdrawRequisites (`/withdraw/fiat/requisites`)
Reached only via WithdrawFiat's route state (redirects back to `/withdraw/fiat` if missing).

**The transfer-type tabs are gone.** They used to re-ask "what kind of transfer is this" via a `SegmentedControl` (Внутренний / Межбанк KG / Межбанк RU) — redundant, since the withdrawal method already picked on the previous screen (WithdrawFiat) determines that: `WithdrawMethod` now carries its own `transferType: FiatTransferType`, derived from the backend's `paymentType` string via `lib/paymentType.ts`'s explicit map (`BAKAI_BUSINESS` → `internal`, `INTER_BANK_KG_BUSINESS` → `kg`, `INTER_BANK_RU_BUSINESS`/`SWIFT` → `ru` — mapped by value, never string-matched on the display label), and this screen just looks it up (`options.methods.find(m => m.paymentType === routeState.paymentType)`). The tabs were also the thing sliding out of alignment on smaller screens — removing them removed the bug along with the redundant question. The field set below is keyed off that looked-up type, unconditionally:

| Transfer type | Fields shown |
|---|---|
| internal | account only |
| kg | account, ИНН, bank name, БИК банка получателя |
| ru | account, БИК получателя, ИНН, bank name, БИК банка получателя, корр. счёт |

**Fields never disappear.** The saved-requisite `Select` (only rendered at all when there's at least one saved requisite matching this method's transfer type — filtered, since a saved entry for a different field shape wouldn't populate sensibly) sits above the field set; picking a saved entry fills the fields in and disables them (still visible, not hidden) instead of swapping the form out from under the user, picking "Выберите реквизиты" (was "Новые реквизиты" — renamed per a Figma annotation showing this as the field's neutral default/placeholder text, not a real option label) clears them back to blank and editable. "Сохранить реквизиты" only shows for a fresh entry — saving an already-saved one doesn't mean anything.

**Screen title is `--text-primary` now** (was `--text-body`, read as dimmer than the rest of the app's screen titles per the same pass). **Buttons are a sticky footer now**, not inline at the end of the scroll (`.withdraw-requisites__submit`, same pattern as WithdrawCrypto/WithdrawFiat's own sticky submit — this screen just hadn't been given one yet). **"Подтвердить" is disabled until the account field has a value** (`disabled={submitting || !account.trim()}`, was only `submitting` — the button used to be tappable from the moment the screen loaded, only failing validation after the fact). `SummaryCard`'s padding is uniform `16px` now (was `14px 16px` — top and sides didn't match). The summary card shows the chosen method's static min/max until a quote comes back, then switches to the quote's own `commission`/`amountToWithdraw` — same "static preview → live numbers replace it" pattern as WithdrawCrypto's SummaryCard, just triggered on Confirm-tap instead of a debounced effect.

Submit (`api-integration.md` §5.2–§5.4, real as of this round — see §8): validate account has a value → build the `RequisitesPayload` (field set per the table above, `saveForLater` set from the checkbox only when entering a fresh requisite) → `getWithdrawFiatQuote({ currency, paymentType, operationOption, amount, requisites })` — `isRequisiteSave` rides along on this same call (§5.4: a new requisite saves inline with the withdrawal, no separate round trip) → `issueWithdrawOtp(quote.transactionId)` picks the modal via its `source` → `TwoFactorGate` → `confirmWithdrawOtp(quote.transactionId, code, requisites)`.

### 6.9 Profile (`/profile`)
Two full-bleed panels, bg-raised frame, 16px seam (see §2.3). `User` now carries `verified: boolean`, `securityLevel: 'LOW'|'MEDIUM'|'HIGH'`, `phone`, `faqUrl`, `aboutUrl` — the old `verificationLevel: 1|2` model was dropped entirely, it's a different concept (verification vs. account-security posture) and the new model matches the SVG mockup this was rebuilt against.

**Panel 1 — account.** Title row: client name + a "Верифицирован" badge (green check, hidden entirely when `!user.verified` — no badge, not a grey one). Details card (`--bg-surface`, 3 stacked label/value rows): Почта (masked, copy icon copies the *unmasked* address), ID пользователя (32 chars grouped by 4, copy icon strips spaces), Телефон. Then the compact certificate button (`Справка об открытии аккаунта`). **No "Уровень безопасности" row and no "Часы деска" section any more** — both removed per explicit request; the security-level row didn't earn its keep and the desk-hours block duplicated Home's hero caption. `User.securityLevel` still exists on the model/fixtures, it's just not rendered here.

**The certificate button opens the sample document directly in mock mode** — the same one-line `openExternalLink(SAMPLE_DOCUMENT_URL)` pattern DealDetail's document rows use (`lib/sampleDocument.ts`, shared by both), no loading spinner, no failure toast. **Real mode is real again as of this round** (`api-integration.md` §8, `real/certificate.ts`'s `getAccountCertificate()`): `POST /certificates/open-wallet` returns a **file body, not a URL** (question B13 flags this as unconfirmed — adapt if it turns out to be a redirect instead), so `Profile.tsx` fetches it via a new `apiFetchBlob` (`api/http.ts`, shares the same header/401-retry plumbing as `apiFetch`, just calls `.blob()` instead of `.json()`), turns it into an object URL, hands that to `openExternalLink`, and revokes it a minute later — long enough for the external browser to have started loading it. `certificateLoading` gates repeat taps and drives the button's `loading` state; a failure shows the generic error toast. Mock and real are genuinely different flows here (mock has nothing to load, real does), so `Profile.tsx` branches on `USE_REAL_API` directly rather than this being a mock/real function pair in `api/index.ts` — same precedent as Google/Apple sign-in (§5).

**Panel 2's `flex` is `1 0 auto`, not the shorthand `flex: 1`.** The shorthand's default `flex-basis: 0%` let the panel — and the `Выход` row pinned to its bottom via `.profile__spacer` — shrink below its own content height on short screens instead of the page growing past 100% and scrolling, so `Выход` ended up clipped below the fold with no way to reach it. `flex-basis: auto` (respect content height) + `flex-shrink: 0` (never compress below it) fixes that while keeping the grow-to-fill-remaining-space behavior on tall screens where there's room to spare.

**Verified badge, copy icon, email mask** — per a Figma annotation pass: the verified glyph was first rebuilt as a filled `--verified` circle with a `--bg-page` checkmark cut out of it (was a bare stroked checkmark with no circle backing, read as too thin), then replaced again with a pixel-exact export — a scalloped seal-badge shape with the checkmark built into the same single path, `currentColor`-filled (still resolves to `--verified`). `CopyIcon` redrawn as the standard two-overlapping-squares glyph. `maskEmail` now shows **3** asterisks (`kar***@gmail.com`) instead of 1. `.profile__detail-text` gained `min-width: 0` — without it the flex child couldn't shrink to trigger its own `text-overflow: ellipsis`, so on the longer ID row the text could push past the card's width and leave the copy icon looking "far away" with inconsistent spacing versus the short email row; with `min-width: 0` both rows truncate correctly and the icon sits at a consistent 8px gap from the text in both.

**Certificate button, setting-row values, tab bar corners** — the compact secondary "Справка об открытии аккаунта" button now fills `--border` instead of the standard secondary `--bg-raised` (scoped to `.button--compact.button--secondary`, which per `Button.tsx`'s own comment is only ever used by this one button, so it's safe to diverge from the shared secondary fill here) — the darker shared fill read as too close to the page background per the same annotation pass. `SettingRow`'s value text (`Русский`, `Тёмная`) is now weight 600 (was inheriting normal), and its trailing chevron flipped from a right-arrow (navigate-deeper semantics) to a down-arrow (opens-a-sheet-below semantics) to match `PickerModal` becoming a bottom sheet. `TabBar`'s top corners are no longer rounded (`border-radius` removed entirely, was `16px 16px 0 0`) per the same pass calling out rounding on "the menu" specifically — this is a global `TabBar` change, so it applies to Home/Deals too, not just Profile. `GlobeIcon`/`ThemeIcon` (Язык/Тема row icons) were redrawn as filled/solid shapes (tinted-fill globe, solid crescent moon) instead of pure outline strokes, matching the same filled-icon direction as the tab bar icons (§6.3).

**Panel 2 — settings.** `Язык` and `Тема` rows now open a `PickerModal` (see §3) instead of showing a static value: Язык offers Русский/English/Кыргызча, Тема offers Тёмная/Светлая, backed by `store/settings.ts` (in-memory zustand, not persisted). Selecting an option updates the row's displayed value immediately. This is still cosmetic — the app stays Russian/dark regardless of what's picked, per the original design intent ("wire it to a store now so a real switch later is a data change, not a screen change") — but it's now a real interactive picker instead of an inert label, per an explicit request. Divider, then FAQ / О нас (external links via `user.faqUrl`/`user.aboutUrl`), then `Выход` pinned to the bottom. **No confirmation dialog any more** — it used to ask "Выйти из аккаунта?" first; tapping it now clears the session and lands on `/login` immediately, per explicit request.

### 6.10 OtcUnavailable (`/otc-unavailable`)
`BlockingState`: 80×80 shield illustration, title/body per reason (`VERIFICATION_REQUIRED` vs `NOT_ELIGIBLE`), action button (open web cabinet / contact support).

### 6.11 TransferModal (global)

Rebuilt this round against the `Трансфер` Figma frame (node `2328-79489`) — the geometry below is measured off that export, not approximated. Opens over any withdrawal screen via the amount-field transfer glyph; the screen underneath stays fully legible (the scrim is only the 10% white wash, `--modal-scrim`, no extra darkening or blur beyond that).

**Card.** 343 wide (`Modal`'s `width={343}` prop, capped by the shared `max-width: calc(100% - 32px)` on narrow screens), radius 16, `--bg-page`, centered. Title `Трансфер между аккаунтами` wraps to two lines. Close control is the bare `×` glyph every modal uses now (§3) — this was the one modal it was originally built for, before the style became the universal default.

**Account fields — stacked, not side by side.** `С` and `На` are two full-width `Select`s (plain layout, `--bg-raised` fill, 44px height) stacked vertically with an 8px gap, **not** side-by-side with a swap button between them (that was the pre-rebuild layout, and it didn't match the frame). The swap control floats centered on the seam between the two fields, overlapping each slightly: a 52px `--bg-page` ring (`.transfer-modal__swap-ring`) containing a 44px `--bg-raised` circular button with a 24px up/down-arrows glyph. The two fields can never hold the same account — picking one flips the other; the swap button exchanges them and leaves the asset/amount alone.

The ring's vertical position is **measured off the DOM, not guessed from a CSS midpoint.** Centering it at 50% of the whole `С`-label+box+`На`-label+box stack lands it much closer to `С` than to `На`, because only `На` has a label sitting in the way — it read as "pinned to the top field" rather than symmetric. `TransferModal.tsx` instead measures the actual `.select__box` elements' `getBoundingClientRect()` (via a `ResizeObserver` on the accounts container, plus a one-off re-measure ~220ms after open to let the modal's scale-in animation settle first — measuring mid-animation was itself good for a few stray px) and sets the ring's `top` to the true midpoint between "С" box's bottom and "На" box's top. This is deliberately dynamic rather than a hardcoded px offset so it stays correct if the label text, font, or locale ever changes.

**Spacing rhythm — three different gaps, not normalized to one.** 8px between the `С` and `На` field groups (the swap ring straddles this seam). **40px** between the account block and the amount block (`Списать`) — they're separate blocks and the frame draws the separation that way. 12px between the two footer buttons (`.transfer-modal__footer`, not the shared `.button-row`'s default 16px). Do not collapse these to a single uniform gap.

**Amount field.** Standard `AmountField` (44px, `--bg-page` fill — the one place besides the account selects where a field's fill matters: `--bg-raised` means "pick a thing", `--bg-page` means "type a number"), with an embedded 24px `CurrencyIcon` asset `Select` and a "Макс" button. A transfer carries no fee, so "Макс" is the full available balance of the source account for the selected asset.

**Footer.** Paired `Отменить` (secondary) / `Подтвердить` (accent) buttons, 12px gap (see above).

**Behaviour** (unchanged by the rebuild): on confirm, the modal closes, a toast confirms, and the screen underneath re-fetches balances — which on the deal hold-confirmation screen can move the callout into a different case, the reason the transfer glyph is reachable from there at all. **Wired for real this round** (`api-integration.md` §6, `real/transfers.ts`): `POST /operations/transfer/internal` with `{currency, accountFrom, accountTo, amount}`, empty 200, no OTP step. The catch: `accountFrom`/`accountTo` are the backend's *own* account keys, not the app's `deposit`/`trading` labels `getAccounts()` remaps them to for display (§6.5/§8) — a new `resolveTransferAccounts()` in `real/balances.ts` maps back from the app's labels to the real keys, reusing the same positional first/second-key convention (question B7) the rest of that file already uses, rather than inventing a second guess at which key is which.

**Still open:** the Figma export doesn't cover the modal's loading, error, or empty-source-account states — those exist in the build (spinner on confirm, insufficient-funds error under the amount field) and stay as they are until a frame exists to check them against.

## 7. Mock/dev switches (`src/lib/devSwitches.ts`, `?param=` on the hash route)

- `?otc=verification|not_eligible|granted` — forces `user.otcAccess`.
- `?scenario=quote_expiring` — the `RATE_ACTIVE` deal's `quoteExpiresAt` starts at ~10s instead of 5 minutes (applied once at the mock store's module init).
- `?scenario=empty_deals` — deals list loads empty.
- BTC has no network selector shown (BTC only ever has one network).

## 8. Known gaps / next

- `Badge`'s `count` variant and `SegmentedControl`'s `count` prop are unused since the deposit-account tab counters were removed; keep or delete next time it comes up.
- Deal list/detail/status-changing actions are all real-API-ready as of this round (see the OTC entries below) — the only gap left is the four `REQUOTE`-specific commands with no screen to call them from yet.
- Profile's language/theme pickers are UI-only (§6.9) — no i18n dictionary swap or light-theme token set exists yet. Building either is a real feature, not a follow-up polish item.
- Worth confirming whether `isRealTelegram` detection is reliable in the actual deployed bot context (§4) — several past layout issues traced back to uncertainty here.
- TransferModal's loading/error/empty-source-account states aren't covered by its Figma frame (§6.11) — they exist in the build as-is, revisit if a frame for them ever shows up.
- **Auth was fully rebuilt against `api-integration.md`, superseding the previous round's `miniapp-auth-integration-spec.md`-based Telegram-binding work entirely** (that whole architecture — `session.start`/`login`/`login-confirm`, no refresh token, silent-relaunch-once-bound, `SessionErrorCode`, `AuthError`, `bootRealSession`, the two-backend `config.ts` — is gone, not reconciled). New shape: `src/api/real/http/` (`servicePaths.ts` — one gateway + per-service prefix/override; `authClient.ts` — isolated basicAuth caller for `/oauth2/*`, question B1; `tokenStore.ts` — access token in memory+sessionStorage, refresh in localStorage, proactive refresh at `expires_in-60s`, serialized reactive refresh on 401; `apiError.ts`/`publicClient.ts`), `src/api/real/errorMap.ts` (code→Russian copy, one place), `src/api/real/auth.ts` (sign-in/social/recovery/sign-out — see §5). The personal/business split (`accountType` body field now, not two backends) is layered on top of `api-integration.md`, which describes one merchant/gateway throughout — this app still runs two account-type flows, but no longer two backends. **No live backend exists yet** — verification stayed at `tsc`/lint/build plus a full mock-mode Playwright pass (both `twoFA` branches of sign-in, and the complete 3-step password-recovery flow) proving the new call shapes work end-to-end on the mock side; the real-mode fetch calls themselves are unverified against an actual server. Withdrawal-confirmation 2FA (`TwoFactorGate`/`VerificationModal`/`AuthenticatorModal`) deliberately *not* touched this round — still points at the old placeholder `sendVerificationCode`/`verifyCode` endpoints, to be replaced once the Withdrawals step wires §5.3's real `issue-otp`/`confirm` contract. Numerous open questions from `api-integration.md` (B1 basicAuth-in-bundle, B2 the `scope` value + how a `twoFaCode` actually travels, B3 OAuth-redirect-into-miniapp reliability, B5 bearerAuth on a public password-recovery endpoint) are implemented as documented best-guesses, each isolated to one function/module per the doc's own instruction, not blocking.
- **Profile + balances wired for real** (`api-integration.md` §3/§4). `real/profile.ts`'s `getUser()` now calls `GET /secured/user/profile` (`userAccount` service) — `clientName`/`securityLevel`/`email`/`userId`/`phone` map directly per §3's table. `verified` has no field in the real contract at all (question B6): derived from a new `real/otc.ts`'s `getOtcAccess()` (`GET /v1/otc/access`, `financial` service) — `.enabled`. That same call also replaces `otcAccess`'s derivation: `OtcAccessReason` gained a fourth value, `DESK_CLOSED` (§7.1 — `available:false` with `enabled:true`, "the desk is closed," a state the old 3-way model had no room for), mapped via a new `lib/otcAccess.ts` (`reasons[]` → the app's existing verification/not-eligible copy, unrecognized codes fall back to not-eligible, per the doc's own instruction — no real reason-code vocabulary is documented anywhere). `OtcUnavailable.tsx` gained a third branch for it (no external link to send the user to, so its action just returns to `/home`); `otcAccessOverride()`'s dev switch gained `?otc=desk_closed` to match. **Known gap, not guessed around:** the real profile response has no `webCabinetUrl`/`supportUrl`/`faqUrl`/`aboutUrl` fields anywhere in §3's mapping table — `real/profile.ts` ships these as empty strings rather than inventing plausible-looking URLs, so Profile's certificate/support/FAQ/about buttons and `OtcUnavailable`'s action link will silently no-op in real mode until the backend confirms where these actually come from.
  - `real/balances.ts` rebuilt from scratch: `Accounts`'s `getAccounts()` no longer has its own endpoint — §4 nests the deposit/trading split inside the *same* `GET /users/balances/{user-id}` response `getAssets()` already needs (`balances: {<accountKey>: {list: [...]}}`), so both now share one `fetchBalancesData()`. Which key is "deposit" vs "trading" isn't documented (question B7) — both functions pick positionally (first key / second key), per the doc's own interim guidance, not a hardcoded string. `TableRow`'s amount reads `availableBalance`, never `totalAmount` (§4: the gap is OTC-frozen funds), and zero-`totalAmount` rows are dropped before the screen ever sees them, matching Home's existing "hidden zero balance" caption logic exactly as before. Asset display names (`Tether`, `Bitcoin`, …) aren't part of the balance item schema, so a small static `ASSET_NAMES` lookup fills that gap — not mock data, the same role as an icon set.
  - Both `getUser()` and `getAssets()`/`getAccounts()` independently re-fetch the profile to get `userPublicId` (`real/profile.ts`'s `getCurrentUserId()`) rather than threading it through every call site — an accepted extra round trip per call, kept simple on purpose; worth caching once it's a measured problem.
  - `getStats()` now lives in `real/otc.ts` (moved from `real/profile.ts`) and hits `/v1/otc/dashboard`, not the old placeholder `/profile/stats` — see the OTC entry below.
- **OTC read path wired for real** (`api-integration.md` §7.1–§7.5), the biggest single reconciliation in this doc — `getDeals`/`getDealById` are gated behind `USE_REAL_API` now instead of hard-wired to the mock store unconditionally; `getStats` moved here too (`/v1/otc/dashboard`, `activeRequests`/`turnover30Days` used, `executedDeals` read and discarded per the doc's own instruction — `turnover30Days`'s `value` type isn't documented as number-vs-formatted-string, so it's coerced to a display string either way).
  - **`DealStatus` gained a real-only ninth value, `RATE_RENEGOTIATING`**, mapped from the backend's `REQUOTE` (new `lib/otcStatus.ts`'s `mapOtcStatus()`, exhaustive over all ten real `StatusEnum` values per §7.6's own proposed table — explicitly flagged "assumption, must be confirmed," question B11, but given as a concrete table to implement, not left open. An unrecognized status renders read-only (mapped to `DECLINED`) rather than actionable, per the doc's own reasoning: "an unrecognised state that offers a button is how a client accepts something nobody intended." `REQUOTE`'s screen is the doc's own explicit MVP call — read-only `StatusHero` + Cancel, since the two real screens it actually needs (accept/reject an adjusted amount, accept/reject a new rate) don't exist: *"designing the two missing screens is a separate task — flag it to the analyst, do not improvise them."* Flagging it here, not improvising further — those two screens are a real, separate design task, not something to guess at from a status name.
  - **DealDetail's actions are wired to the real commands** (§7.6) — the `readOnly`-everywhere gating from the previous round is gone, replaced by five named action functions in `api/index.ts` (`acceptQuote`, `rejectQuote`, `confirmHold`, `requestNewRate`, `cancelDeal`), each branching mock/real internally. Named for the UI action rather than the raw command, since one button implies different commands on different screens — both RATE_ACTIVE's and AWAITING_FUNDS's "Отклонить" are `REJECT_QUOTE`; RATE_PENDING's and `RATE_RENEGOTIATING`'s "Отменить заявку" are `CANCEL`. `sendOtcCommand` (`real/otc.ts`) posts the command, then **always refetches the deal** rather than trusting the command response's own `{operationId, status}` shape, per §7.6's explicit instruction (`detailsStatus`/amounts may also have moved). A `409` ("someone moved the deal underneath the user") is handled specifically — swallowed, toasted ("Статус сделки изменился"), still refetches — never a generic error, per the doc's own instruction. The four commands with no built screen to call them from (`ACCEPT_AMOUNT`/`REJECT_AMOUNT`/`ACCEPT_REPRICE`/`REJECT_REPRICE`, `RATE_RENEGOTIATING`'s real accept/reject step) simply have no function — nothing in the app can call them yet.
  - **`RATE_RENEGOTIATING`'s two real triggers, confirmed directly by the user** (not inferred from the spec): requesting a new rate off a stale quote (`RATE_STALE`'s "Запросить новый курс"), and confirming a hold on an amount well short of the deal (`ConfirmationBody`'s "short" branch, beyond `short1`'s ≤1% auto-recalculation). Both used to land back in `RATE_PENDING` in mock mode — now correctly land in `RATE_RENEGOTIATING`, matching what the real commands actually produce (`mockActions.requestNewRate`, and `ConfirmationBody`'s own `short`-branch patch). This makes the new status genuinely reachable and demoable in mock mode, not just a code path nobody can see.
  - `AWAITING_FUNDS`'s shortfall branch (`sufficient`/`short1`/`short`/`belowmin`) deliberately still computes client-side from `getAccounts()` in *both* modes — §7.4 says the server now pre-computes `balance`/`missingAmount` so the client doesn't have to, and `Deal` gained those two fields to carry them, but wiring the branch logic to *use* them needs an `otcMinimum` value from `/v1/otc/access`'s `limits` field, whose shape isn't documented anywhere — rather than guess a second undocumented shape on top of an already-guessed one for a money-adjacent branch decision, the existing (correct, just not server-optimized) client computation was left alone. Revisit once `limits`' shape is confirmed. Real mode's `confirmHold` still passes this client-computed patch through as `mockPatch` (unused by the real path, which just sends `CONFIRM_HOLD` and refetches) — kept as one shared function signature rather than diverging mock/real call shapes for this.
  - `QuoteCard`'s countdown-hits-zero handling now differs meaningfully by mode: mock still calls `expireQuote` (client-side, sets `RATE_STALE` locally); real mode has no command for "the countdown ran out" at all (not one of the nine) — it does a one-off `getDealById` refetch instead, the closest approximation until polling (§7.7, still not built) lands.
  - **`Deal.id` vs `Deal.requestNumber`**: real deals separate the uuid used for every call (`operationId` → `Deal.id`) from the display number ("OTC-1047" → `Deal.requestNumber`, new field, mock leaves it `undefined` and both `DealRow` and DealDetail's header fall back to `id`) — §7.3's own note: "keep both."
  - **Documents**: real mode reads `Deal.documents` (embedded in the detail response per §7.5, preferred over the separate `/documents` call to save a round trip) instead of the mock's static per-status availability table (`lib/dealStatus.ts`'s `getDocumentAvailability`, now mock-only). `nameKey` → display name goes through a small lookup (`ACCEPT`/`PAYMENT`/`CERTIFICATE`, the mock's own three names as the most likely real vocabulary) with a raw-key fallback, per §7.5: "do not render the raw key." `href` opens via `openExternalLink`, replacing the shared static sample PDF only in real mode.
  - `Deal.date` is empty in real mode's detail response (§7.4 doesn't include `createdAt` — only the list does, and detail can be deep-linked without ever fetching the list first) — the "Дата создания" row just doesn't render rather than showing blank.
  - Polling (§7.7) is **not built** — the next real step is withdrawals, then transfer+certificate (`api-integration.md` §9).
  - Verified via a full mock-mode Playwright pass: Home and Profile render unaffected (mock fixtures don't route through any of this), and all four `?otc=` override values reach `/otc-unavailable` with the right copy, including the new `desk_closed` branch. No live backend exists to verify the real fetch shapes against.
- **Crypto withdrawal wired for real** (`api-integration.md` §5.1–§5.3); fiat withdrawal is not — see below. This is the biggest shape change any withdrawal screen has had: the old model was "fetch static per-asset limits once, submit once at the end." The real model is "fetch a network-by-network directory, request a **live quote** as the user fills the form, confirm through that quote's own `transactionId`."
  - `CryptoWithdrawOptions.networks` changed from a flat `string[]` of network names to `WithdrawNetworkOption[]` — each network now carries its *own* `minimalAmount`/`maximumAmount`/`commissionPercent`/`commissionFixed`/`addressRegex`/`contractAddress` (§5.1: "Each item carries everything the form needs before the user types anything"). The old one-flat-`limits`-object-per-asset shape (`CryptoWithdrawLimits`) is gone — mock fixtures (`MOCK_WITHDRAW_CRYPTO_NETWORKS`) rebuilt to match, one row per (currency, network) pair instead of one row per currency. The network `Select` hides itself when a currency has exactly one network (§5.1's own instruction) — this now falls out naturally from `networks.length`, not a hardcoded BTC special case the way it used to be.
  - **New: a live quote, debounced 500ms after address+amount+network are all set** (`getWithdrawCryptoQuote`, new `WithdrawQuote` type) — this is what fills the SummaryCard now (min/limit/commission/final amount), not the static directory numbers. `expiredAt` is watched with its own timer; the quote just clears (and the Confirm button disables) once it passes, per §5.2's explicit instruction to re-request rather than let a stale quote through.
  - **New: `addressRegex`-driven client-side address validation** on blur — §5.1 asked for this and it didn't exist before at all (any string was accepted). A malformed regex is treated as "no validation" rather than crashing the screen, per the doc's own hedge.
  - **The three shared 2FA-modal components got a real prop-shape change, not just a new real implementation.** `TwoFactorGate`/`VerificationModal`/`AuthenticatorModal` used to hardcode a call to the generic mock-mirroring `verifyCode`/`sendVerificationCode` pair internally. Real withdrawal OTP needs a `transactionId` — the quote's — threaded through the verify call, which a hardcoded internal import can't do. So all three now take injected `onSubmit(code)`/`onResend()` callbacks instead, matching the pattern `OtpConfirmModal` already proved out for sign-in/recovery. A new `RateLimitedError` (`lib/rateLimitedError.ts`) is how a caller signals "this specific failure should show the longer lockout state" generically, replacing a hardcoded `instanceof MockVerifyCodeError` check inside the modal.
  - **The account-level `authenticatorEnabled` flag is no longer what picks the modal for withdrawals.** `issueWithdrawOtp(transactionId)` returns the *operation's own* `source` (`email`/`authenticator`/`phone`) — §5.2's own instruction to prefer this over the account-level flag "when present," and for withdrawals it's always present. `phone` maps onto the same `VerificationModal` UI as `email` (no separate SMS-code screen exists — visually identical code-entry, just a different delivery channel, not worth a fourth UI variant for).
  - Verified via a full mock-mode Playwright pass: live quote populating real numbers (min/limit/commission/final all correct against the mock fixture's math), BTC's single network correctly hiding the selector, the business account showing `AuthenticatorModal` and the personal account showing `VerificationModal` (driven by the new operation-level `source`, not the old account flag — both accounts still happen to agree with their `authenticatorEnabled` value in the mock, but the *code path* choosing the modal is different now), and the full submit-through-success flow. Fiat withdrawal regression-checked and unaffected.
- **Fiat withdrawal wired for real** (`api-integration.md` §5.1–§5.4), completing the Withdrawals step. Same directory/quote/OTP shape as crypto (§8's crypto entry above), reused rather than reinvented:
  - **`WithdrawMethod`/`FiatWithdrawOptions` restructured the same way `WithdrawNetworkOption` was for crypto** — one entry per (currency, `paymentType`, `operationOption`) triple, each carrying its own `minimalAmount`/`maximumAmount`/`commissionPercent`/`commissionFixed` (§5.1), replacing the old flat one-`limits`-object-per-currency shape (`FiatWithdrawLimits`, deleted). `real/withdrawals.ts`'s `getWithdrawFiatOptions` and `getWithdrawCryptoOptions` now share one `GET /v3/configuration/withdraws` call (`getWithdrawConfiguration()`) — the response carries `fiatCurrencies`/`cryptoCurrencies` side by side, not two endpoints.
  - **New: `lib/paymentType.ts`'s `paymentTypeMeta()`** — an explicit map from the backend's `paymentType` enum strings (`BAKAI_BUSINESS`/`INTER_BANK_KG_BUSINESS`/`INTER_BANK_RU_BUSINESS`/`SWIFT`) to `{ transferType, name }`, per §5.1's explicit instruction not to string-match on display labels. `SWIFT` isn't named in the doc's own list but was already this app's USD method (same `ru` field set as Межбанк RU) — kept as a fourth entry since the doc introduces its list with "includes," not "is exactly."
  - **WithdrawFiat's summary card is a static preview** (computed from the chosen method's own fields), unlike crypto's live-debounced one — the live quote only fires once, on WithdrawRequisites, at Confirm-tap (§6.8): a multi-field bank-requisites form doesn't suit a per-keystroke debounce the way a single address field does.
  - **`getWithdrawFiatQuote`** (`POST /v2/operations/withdraw/info`, same endpoint crypto's quote uses — `paymentType`/`operationOption` in place of `currencyNetworkId`, `requisites` is the full bank-details object instead of `{ address }`) carries `isRequisiteSave` from the form's own "Сохранить реквизиты" checkbox (§5.4 — a new requisite saves inline with the withdrawal, no separate create-then-reference round trip).
  - **`confirmWithdrawOtp`'s `address?: string` param generalized to `requisites?: object`**, shared verbatim by both asset kinds now (crypto passes `{ address }`, fiat passes the same `RequisitesPayload` sent at quote time) — one signature across `api/index.ts`, `real/withdrawals.ts`, and both withdrawal screens, rather than a second near-identical function.
  - Resolves **question B8** (`amountToWithdraw` vs `finalAmount` ambiguity, flagged but left open in the crypto round): for fiat, the entered amount debits the account and the fee comes out of it, so `amountToWithdraw = amount + commission` (what leaves the account) and `finalAmount = amount` (what the recipient sees) — implemented in the mock quote to match, real mode trusts the backend's own numbers either way.
  - Verified via a full mock-mode Playwright pass: the complete directory → amount → requisites → quote → OTP → success flow for a KGS/internal withdrawal (screenshots confirmed the saved-requisites picker, the account-only field set, and the email-OTP modal opening off the operation's own `source`), zero console errors. Crypto withdrawal regression-checked end-to-end after the `confirmWithdrawOtp` signature change and still passes clean.
- **Transfer and certificate wired for real** (`api-integration.md` §6/§8) — the "two small ones" from the doc's own §9 work-plan step 7, and the last piece of that numbered plan besides OTC polling. Full detail in §6.11 (TransferModal) and §6.9 (Profile's certificate button) above; summary: `transfer()` now resolves the app's `deposit`/`trading` labels back to the real backend's own account keys before calling `POST /operations/transfer/internal` (no OTP step, empty 200 response); the certificate button fetches a real file body via a new `apiFetchBlob` helper (`api/http.ts`) and opens it as a blob URL in real mode, unchanged in mock mode. Neither touched any mock-mode code path — verified via a full mock-mode Playwright pass (Profile screen renders and the certificate button still opens the sample PDF instantly with no loading state; a full transfer through `TransferModal` opens, submits, and closes cleanly) plus careful reading against the spec for the real paths, no live backend to verify those against.
- **OTC polling wired for real** (`api-integration.md` §7.7) — the last step of the doc's own numbered plan. New shared `lib/usePolling.ts` hook: fires `callback` on a `setInterval`, no-ops each tick while `document.hidden` rather than tearing the interval down and rebuilding it, and fires once immediately on `visibilitychange` → visible so a client who just tabbed back in doesn't wait out a full interval for the truth. `Deals.tsx` polls the list every 30s while real mode is on (`USE_REAL_API`, no status concept for a list); `DealDetail.tsx` polls the open deal every 10s, gated additionally on the deal being in a non-terminal status (new `lib/dealStatus.ts`'s `isTerminalDealStatus()` — `DONE`/`DECLINED`, the app-level statuses `EXECUTED`/`CANCELED`/`ERROR` map to). Mock mode is untouched — nothing server-side ever changes a mock deal out from under the user, so polling is gated off entirely there, not just harmless-but-pointless. `QuoteCard`'s existing "refetch once when the countdown hits zero" one-off stays alongside the new 10s poll rather than being replaced by it — it's the snappier path for that one specific moment, the poll is the general safety net. Verified via a full mock-mode Playwright pass (Deals list and DealDetail both render correctly with polling gated off, zero console errors) plus careful reading against §7.7 for the real-mode behaviour, no live backend to verify polling traffic against.
- **The user supplied a paired test-readiness document** (`Тест-план_вопросы_к_бэку_вывод_в_прод.md`, business-analyst-facing, same `B#` codes as `api-integration.md`) — three new questions not in the original spec: `B14` (no `availableCommands` field on a deal, client has to re-derive which of the nine commands are currently valid from status alone), `B15` (rate limits on 10s/30s polling at scale), `B16` (a test stand that can move OTC statuses without a live operator). Not yet coded against — flagging here so they're not lost; `B14` in particular would simplify `lib/otcStatus.ts` if answered.
- **Every real-mode error message now carries a short request-id code** (`mapApiError`, `real/errorMap.ts`) — previously only the unmapped-code fallback did (`"Что-то пошло не так... (a1b2c3d4)"`); known-code messages (`"Неверный email или пароль"` etc.) showed clean copy with no code at all. The test plan's own requirement (§1/§9.8): "показывать на экране ошибки короткий код запроса ... без него баг 'не сработало' невозможно найти в логах" — every error a tester or a real client hits needs to be traceable back to a server log line. Also removed `SignIn.tsx`'s two `httpStatus === 401/429` ternaries, which bypassed `mapApiError` entirely (and so never showed a code) to construct copy straight from the HTTP status — `errorMap.ts`'s own header comment already said "screens never construct their own copy from a code," and its `KNOWN` map already had `INVALID_CREDENTIALS`/`TOO_MANY_ATTEMPTS` entries with near-identical copy, so the bypass was redundant as well as wrong; both call sites now just call `mapApiError(err)` like every other screen already did. The now-dead `ru.signIn.errorCredentialsInvalid`/`errorTooManyAttempts` keys came out with it. `PasswordRecovery.tsx`/`NewPassword.tsx` already routed everything through `mapApiError` and needed no change. Deliberately left alone: OTC's `409`/`404` handling (`real/otc.ts`) — those aren't generic errors, they're specific state transitions (`"Статус сделки изменился"` toast, silent not-found) the doc explicitly says should read as normal, not as a fault.
- **Google/Apple sign-in temporarily hidden** — see §5's own entry above.
- **AWAITING_FUNDS's confirm button relabeled** — "Подтвердить сделку" → "Подтвердите оплату" (`ru.dealDetail.confirmPaymentAction`, new key), explicit instruction: this status is specifically about paying in, not accepting a rate, so it gets its own label rather than sharing `confirmDealAction` with RATE_ACTIVE's quote-accept button (unchanged, still "Подтвердить сделку" — same key, same screen-adjacent button, genuinely different action). **`ru.dealDetail.historyAction` ("История операций") removed outright** — dead already (no screen ever rendered it, grep confirmed zero JSX references), and per explicit instruction the mini app will never have a transaction-history screen at all, so there was nothing to wire it to going forward either.
